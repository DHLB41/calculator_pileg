# Requirements for Simulasi Kalkulasi Pemilu 2029

# Streamlit for web app interface
streamlit==1.33.0

# Pandas for data manipulation
pandas==2.2.2

# Optional: If you want advanced HTML to PDF export in the future
weasyprint==61.2

# (Already used internally) Additional supporting libraries
# Note: These libraries are part of Python Standard Library or built-in, so no need to list:
# - datetime
# - os

# Optional for local development
# python-dotenv==1.0.1  # (if someday using environment variables)

# === Notes ===
# Make sure to install these packages in your virtual environment
# Installation command:
# pip install -r requirements.txt
